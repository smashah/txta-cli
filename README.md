# txta.dev CLI

Send an encrypted letter to a developer's GitHub profile repository from your own machine and GitHub account.

```bash
npx txtadev smashah hi
```

The canonical package is also directly runnable:

```bash
npx @txtadev/cli
```

The CLI takes a GitHub username and message, uses the first published SSH encryption key GitHub returns, encrypts locally, and creates one immutable issue in the recipient-owned `username/username` profile repository through your authenticated [`gh`](https://cli.github.com/) session. Leave either value out and txta prompts for it; pipe a longer message over standard input if you prefer.

Read your letters without finding a key filename or installing `age`:

```bash
npx txtadev inbox
npx txtadev read 6
```

`inbox` lists the sealed issues in your GitHub profile repository and asks which issue number to open. `read` extracts that issue's expected key fingerprint, tries the matching key first, then tries every supported private key in `~/.ssh`; it asks for a passphrase only when a locked key needs one.

If GitHub publishes several keys, choose the one whose private half is on your current machine:

```bash
npx txtadev set
```

`set` intersects GitHub's public SSH keys with the usable private keys in `~/.ssh`, asks you to choose, then commits only the selected fingerprint to `.github/txta.json` in your public `username/username` profile repository. Senders use that preference automatically. The profile repository remains the authenticated source of truth; txta.dev may cache the public fingerprint later, but it never needs your GitHub token or private key.

## Requirements

- Node.js 22 or newer
- GitHub CLI authenticated as the sender: `gh auth login`
- The recipient publishes an `ssh-ed25519` or `ssh-rsa` key and has Issues enabled on their public profile repository
- Opening a letter requires the corresponding private key on the recipient's machine or backup; publishing a replacement key only helps future letters

## Non-interactive handoff from txta.dev

The command generated by [txta.dev](https://txta.dev) passes the draft over standard input, where the CLI encrypts it before GitHub receives anything:

```bash
npx txtadev octocat <<'TXTA_LETTER'
Your message
TXTA_LETTER
```

## What receives the message

txta posts one issue titled `📬 You've got mail` to `recipient/recipient`. The issue contains the txta.dev letter renderer and one AGE envelope. The bundled reader opens it locally with the matching SSH private key; txta.dev has no backend in the sending path and never receives the draft, ciphertext, or GitHub credential.

## Package structure

- `@txtadev/cli` owns all implementation.
- `txtadev` is a minimal forwarder pinned to the exact same version.
- Changesets keeps both packages in one fixed release group, and `scripts/check-lockstep.mjs` rejects version or dependency drift.

Publication runs only through the protected GitHub Actions release workflow. The workflow consumes pending changesets, commits the fixed pair's lockstep version, verifies both packed commands, publishes through npm OIDC, and pushes the release tags.
