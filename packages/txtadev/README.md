# txtadev

The short command for the [`@txtadev/cli`](https://www.npmjs.com/package/@txtadev/cli) implementation.

```bash
npx txtadev smashah hi
```

This package contains no product logic. Its exact-version dependency forwards directly to the canonical CLI.
