---
"@txtadev/cli": patch
"txtadev": patch
---

Choose a decryptable recipient key with `txtadev set`. The command intersects public GitHub keys with local private keys, publishes only the selected fingerprint in the user's profile repository, and makes future sends honor that preference.
